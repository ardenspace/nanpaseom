# placeholder 에셋 홈

디자이너가 **같은 파일명으로 덮어쓰기** 하는 드롭인 디렉토리.
Vite 빌드 시 `dist/assets/` 로 그대로 복사되어 `/assets/<name>` 으로 서빙된다.

파일명 규약 (코드가 이 이름을 참조하므로 이름 변경 금지):

| 파일 | 용도 |
|---|---|
| `surigong.png` | 수리공 스프라이트 |
| `bg.png` | 배경 (어둑한 바다/난파섬) |
